#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include "../main.h"

static char array[50][50];

int edgedetect(FRAMECHAR *CurrentFrame)
{
    int rating;
   //CurrentFrame->framebits  // A RGB matrix of pixel values Values are Stored G B R G B R in a 2-D array
  //  CurrentFrame->width // Width
  //  CurrentFrame->height // Height
  //  CurrentFrame->pos  //frame number

  return(rating);
}
